<div class="hero">
  <img src="/assets/img/burger.png" style="width:200px;height:200px;"alt="Burger Illustration">
<h1>Eat-Da-Burger!</h1>

</div>

<h2>Burgers that WANT to be Devoured</h2>
<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These burgers were DEVOURED!!!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<div class="addBurger">
  <h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerName">Burger Name:</label>
    <input type="text" id="burgerName" name="burgerName">
  </div>

  <button id="addBurger" type="submit">Add Burger</button>
</form>
</div>
